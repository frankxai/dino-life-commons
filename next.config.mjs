/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [],
  },
  experimental: {
    // keep lean for storage-constrained machines
  },
}

export default nextConfig
